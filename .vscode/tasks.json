{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Python: Main File",
            "type": "shell",
            "command": "${command:python.interpreterPath} ${workspaceFolder}/app.py",
            "args": [],
            "group": "build"
        }
    ]
}
